#include <stdio.h>
#include <string.h>
#include <stdbool.h>

int main()
{
/*
   char c;
   while((c=getchar())!=EOF)
   {
       putchar(c);
   }
*/

   char c[30] = "我的堪我的我的我的我的";
    int a_number[3] = {1, 2, 3};
    /*
   if(a_number[1] == 0)
       printf("%s", "Null Found");
    int str_len = sizeof(c);
    printf("%d", str_len);
     */
    printf("%s", NULL);

   return 0;
}
